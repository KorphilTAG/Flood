interface:
  display_name: "Feature Pipeline"
  short_description: "Plan and implement a feature against the PRD"
  default_prompt: "Run the feature pipeline for: "

policy:
  allow_implicit_invocation: false
