Implement exactly the spec at the path below. Read it in full first, then read `docs/specs/physics-engine-cycles.md` (sections "File ownership" and "Interface freeze"), `src/flood/interfaces.py`, `src/flood/engine/cube.py`, `tests/conftest.py`, `tests/fixtures/mini_huc/build.py`, and every existing file the spec names. Follow "Files to change" and "Implementer notes" literally: exact paths, signatures, constants, and test names. Never edit `src/flood/interfaces.py`, `src/flood/engine/cube.py`, `tests/conftest.py`, or anything under `tests/fixtures/`. Do not edit files you do not own; if you need a change elsewhere, record it under Residual risk in changes.md. No scenario constants in `src/`. Do not commit, stage, or run any git command that changes state.

Spec: pipeline/features/routing-forecast/spec.md

Environment:
- You are at the root of a git worktree on branch `feature/routing-forecast`. Windows 11; use PowerShell syntax for shell commands.
- First create the environment: `py -3.12 -m venv .venv` then `.venv\Scripts\python.exe -m pip install -e ".[dev]"`. Use `.venv\Scripts\python.exe` for everything, including `-m pytest -q`.
- Copy `pipeline/templates/changes.md` to `pipeline/features/routing-forecast/changes.md`, fill the Slug and Spec lines, and keep it updated as you work.
- Cycles C02, C03, C04, C08 are being implemented concurrently. `flood.engine.rating` and `flood.engine.forcing` do not exist in this worktree and must not be imported. Instead:
  - In `routing.py`, implement private helpers with the exact definitions from the C03 spec (`pipeline/features/hand-mapping-core/spec.md`, section Acceptance criteria): `_stage_from_q(rt, cidx, q) -> (stage, clipped)` by `np.interp` on that catchment's `q_cms` row to `stage_m`, clamped to the top row; `_wet_area`, `_top_width` by interpolation on stage; `_celerity(rt, cidx, q)` as dQ/dA by central differences of `q_cms` over `wet_area_m2`, evaluated at `q`, clipped to `[0.1, 10.0]`. Put `# TODO(fix-up): replace with flood.engine.rating once C03 merges` above them.
  - In `tests/test_routing.py`, implement `FixtureForcingView` satisfying the `ForcingView` protocol from the fixture Parquet files under `mini_data_dir` (`usgs/mini-huc/continuous.parquet`, `nwm/mini-huc/analysis.parquet`, `nwm/mini-huc/short_range.parquet`): `obs_q(site)` rows with `parameter == "00060"` and `valid_time + 5 min <= p`; `obs_wse` likewise for `00065`; `nwm_analysis(fid)` rows with `valid_time + 60 min <= p`; `latest_short_range(fid)` the cycle with the greatest `issue_time` having `issue_time + 90 min <= p`; `qlat(fid, tau)` the analysis `qlat_cms` at the last known `valid_time <= tau` (else the last known value, else 0.0) times `ratio(fid)`; `ratio(fid)` computed as observed over analysis discharge at the latest time both are known for the gauge on reach 101 when `fid` is on levelpath 9, else 1.0. Keep it under 80 lines.
- Signature clarification: `route(cube, view, scenario, config, members=MEMBERS, max_horizon_minutes: int = 360, warmup_minutes: int = 360) -> RoutedSeries`, where `config` is the `ForcingConfig` model (`mini_scenario.forcing_defaults` in tests). Clamp the warm-up start to `scenario.hydrology.record.start` and drop `taus` after `record.end`. Gauge roles come from `scenario.hydrology.gauges`; gauge sites per reach from `cube.network.gauge_site`; junction inferences from `scenario.hydrology.junction_inferences`.
- Fixture facts you will rely on: gauge 90000001 on reach 101 (role boundary), 90000003 on 103 (interior), 90000005 on 105 (interior); observed `q1(t) = 5 + 45*tri(t)` rising 02:00 to 05:00 and falling to 10:00; `q3(t) = 2*q1(t - 10 min) + 3`; `q5(t) = 0.98*q3(t - 20 min)`; analysis is `0.5 * truth`; junction inference: reach 102 = obs(90000003, tau + 10 min) - obs(90000001, tau).
- Tests, stated plainly, all with `p = 2025-01-01T04:00:00Z` unless noted:
  - `mc_step` steady state and coefficients sum to one; reach-level mass conservation: route a triangular hydrograph with zero baseflow through one reach with fixed `K = 600 s`, `X = 0.3`, `dt = 60 s` over a window long enough to drain, and assert `sum(out) * dt` within 0.5 percent of `sum(in) * dt`.
  - Controls: `q[mid]` at reaches 101 and 103 equals the fixture observation within 1e-4 at every `tau <= 03:55Z`.
  - Lag: with `p = record end` (hindsight), the peak time of `q[mid]` at reach 104 is later than at reach 103 by an amount within 50 percent of `1000 / c` where `c` is `_celerity` at reach 104's peak discharge.
  - Members: at reach 101 for `tau > 03:55Z`, `q[low] <= q[mid] <= q[high]` with strict inequality at `tau = 04:30Z`; all three equal for `tau <= 03:55Z`.
  - Junction: reach 102 at `03:00Z` equals `obs3(03:10Z) - obs1(03:00Z)` within 1e-3 and has source code `mass_balance`.
  - Sources: 101 is `observed` before `t_last` and `forecast_trend` after; 104 is `routed` throughout; 103 is `observed` then `routed`.
  - Hindsight: with `p = record end`, no `forecast_trend` code appears.
  - Runtime: one `route` call on the fixture completes in under 2 seconds.
- Acceptance criteria are the definition of done. Run pytest until green offline. If a criterion cannot be met, keep the test, mark it xfail with a reason, and record it under Not done.
- Finish: run `.venv\Scripts\python.exe -m pytest -q` and paste the output into changes.md under "How to verify".

Work order: `boundary.py` with tests; `mc_params` and `mc_step` with tests; `route` with the `FixtureForcingView` and the network tests above.
