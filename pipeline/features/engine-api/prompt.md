Implement exactly the spec at the path below. Read it in full first, then read `docs/specs/physics-engine-cycles.md` (sections "File ownership" and "Interface freeze"), `src/flood/interfaces.py`, `docs/contracts/contract-1-physics-products.md` section 9, `docs/contracts/examples/state-response.json`, `src/flood/api/clock.py`, `src/flood/clock/service.py`, `src/flood/products/raster.py`, and every existing file the spec names. Follow "Files to change" and "Implementer notes" literally. Never edit `src/flood/interfaces.py`, `src/flood/api/clock.py`, `src/flood/clock/*`, `tests/conftest.py`, anything under `tests/fixtures/`, or any engine module. If you need a change elsewhere, record it under Residual risk in changes.md. No scenario constants in `src/`. Do not commit, stage, or run any git command that changes state.

Spec: pipeline/features/engine-api/spec.md

Environment:
- You are at the root of a git worktree on branch `feature/engine-api`. Windows 11; use PowerShell syntax for shell commands.
- First create the environment: `py -3.12 -m venv .venv` then `.venv\Scripts\python.exe -m pip install -e ".[dev]"`. Use `.venv\Scripts\python.exe` for everything, including `-m pytest -q`.
- Copy `pipeline/templates/changes.md` to `pipeline/features/engine-api/changes.md`, fill the Slug and Spec lines, and keep it updated as you work.
- Cycle C06 is writing `flood.engine.run` concurrently; it does not exist in this worktree. Code against this frozen surface and inject the store into the app factory so tests can pass a fake:
  - `create_app(settings: Settings | None = None, store=None) -> FastAPI`. When `store` is None, `create_app` tries `from flood.engine.run import RunStore` and builds `RunStore(settings.runs_dir, settings.data_dir)`; if that import fails it logs a warning and serves scenarios, clock, and verifier only, with every `/runs` route returning 503 `{"error": {"code": "engine_unavailable", ...}}`.
  - `RunStore`: `list() -> list[dict]` (manifest dicts), `get(run_id) -> Run` (raises `KeyError` when unknown), `create(scenario, mode, overrides=None) -> Run`, `exists(run_id) -> bool`, attribute `url_base`.
  - `Run`: `run_id`, `run_dir: Path`, `manifest: dict`, `record_start`, `record_end`, `max_horizon_minutes`; `state(p, t, write=False) -> tuple[StateArrays, dict]` where `p` may be an ISO string, a datetime, or the literal `"hindsight"`, and the dict is the contract state response; `reaches(p, t) -> pd.DataFrame`; `gauges(p) -> pd.DataFrame`; `tte(p, write=False) -> np.ndarray`; `product_path(kind, p, t=None) -> Path` for `kind` in `raster`, `reaches`, `time_to_exceedance`, `gauges`. `flood.timegrid.TimeGridError` (with `.code`) propagates from `state`, `reaches`, `gauges`, `tte` on bad `(p, t)`.
- Write `tests/fakes/__init__.py` and `tests/fakes/fake_run.py` implementing `FakeRunStore` and `FakeRun` with exactly that surface on the `mini_huc` fixture grid: `state` returns a `StateArrays` of zeros with a small wet block plus a response dict copied from `docs/contracts/examples/state-response.json` with `run_id`, `p`, `t`, `mode`, `horizon_minutes` substituted; `product_path` writes real files on demand using `flood.products.raster.write_depth_cog` and `write_tte_cog` so the raster, range, and tte routes are exercised for real; `reaches` and `gauges` return frames shaped like `docs/contracts/examples/reaches.sample.json` and `gauges.sample.json`. `FakeRun.state` raises `TimeGridError("t_before_p", ...)` when `t < p`, `TimeGridError("horizon_exceeded", ...)` when `t - p > 360 min`.
- Clock: in `create_app`, build `ClockService(record_start, record_end)` from `FLOOD_CLOCK_START` and `FLOOD_CLOCK_END` environment variables if set, else from the first valid scenario's `hydrology.record`, else a one-day window starting now; `app.include_router(make_clock_router(service), prefix="/clock")`; call `service.start()` in the FastAPI lifespan startup and `service.stop()` on shutdown. The router's own paths are `""`, `""` (POST), `"/reset"`, `"/ws"`.
- Overlay route: read the requested band from the `StateArrays` returned by `run.state(p, t)` (band names in `flood.interfaces.RASTER_BANDS`) and call `flood.products.raster.render_overlay_png(array, grid, max_px)`; grid comes from `run.manifest["grid"]` (build a `Grid` from it) or from `run.cube.grid` when present. Header `X-Bounds-3857` is `f"{xmin},{ymin},{xmax},{ymax}"`.
- Static files: implement `GET /runs/{run_id}/products/{path:path}` and `GET /runs/{run_id}/hindsight/{path:path}` resolving inside `run.run_dir`, rejecting any resolved path outside it (404 `not_found`), and serving with `FileResponse`. Implement byte-range support yourself for `raster` and these static routes: parse `Range: bytes=a-b`, return 206 with `Content-Range` and `Content-Length`, 416 when unsatisfiable, `Accept-Ranges: bytes` on every file response.
- Mount `/verifier` with `StaticFiles(directory=<package dir>/verifier/static, html=True)` only if that directory exists (it is being written by C09 concurrently).
- Every error body is exactly `{"error": {"code": str, "message": str}}`. Map `TimeGridError` to 400 with its code, `KeyError` from `store.get` to 404 `unknown_run`, unknown scenario to 404 `unknown_scenario`, missing `p` or `t` to 400 `missing_parameter`, invalid overrides to 400 `invalid_forcing`, `hindsight` on the tte route to 400 `hindsight_has_no_tte`, unknown band to 400 `unknown_band`, missing skill file to 404 `skill_not_computed`, other exceptions to 500 `internal`.
- Tests in `tests/test_api.py` run against `create_app(settings, store=FakeRunStore(...))` and cover every route and every error code above. Add a second parametrisation that uses the real `RunStore` on the fixture when `flood.engine.run` imports, guarded by `pytest.importorskip("flood.engine.run")`; it will skip in this worktree.
- Add exactly one line under the `# REGISTER:` marker in `src/flood/cli.py` registering `flood.cli_serve` (`flood serve [--host 127.0.0.1] [--port 8000]`), in the same style as the existing lines.
- Acceptance criteria are the definition of done. Run pytest until green offline. If a criterion cannot be met, keep the test, mark it xfail with a reason, and record it under Not done.
- Finish: run `.venv\Scripts\python.exe -m pytest -q` and paste the output into changes.md under "How to verify".

Work order: `api/settings.py`, `api/errors.py`; `tests/fakes/fake_run.py`; `api/scenarios.py`; `api/runs.py` (state, reaches, gauges, raster with ranges, tte, overlay, hindsight, skill, static products); `api/app.py` with clock and verifier mounts; `cli_serve.py`; `tests/test_api.py`.
