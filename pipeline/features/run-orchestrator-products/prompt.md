Implement exactly the spec at the path below. Read it in full first, then read `docs/specs/physics-engine-cycles.md` (sections "File ownership" and "Interface freeze"), `src/flood/interfaces.py`, `docs/contracts/contract-1-physics-products.md`, `docs/contracts/examples/run.json`, `docs/contracts/examples/state-response.json`, and every existing file the spec names. Follow "Files to change" and "Implementer notes" literally: exact paths, signatures, constants, and test names. Never edit `src/flood/interfaces.py`, `src/flood/engine/cube.py`, `tests/conftest.py`, anything under `tests/fixtures/`, or any module owned by another cycle (`rating.py`, `mapping.py`, `ensemble.py`, `forcing.py`, `routing.py`, `boundary.py`, `products/raster.py`, `ingest/*`, `clock/*`, `api/*`). If you need a change elsewhere, record it under Residual risk in changes.md. No scenario constants in `src/`. Do not commit, stage, or run any git command that changes state.

Spec: pipeline/features/run-orchestrator-products/spec.md

Environment:
- You are at the root of a git worktree on branch `feature/run-orchestrator-products`. Windows 11; use PowerShell syntax for shell commands.
- First create the environment: `py -3.12 -m venv .venv` then `.venv\Scripts\python.exe -m pip install -e ".[dev]"`. Use `.venv\Scripts\python.exe` for everything, including `-m pytest -q`.
- Copy `pipeline/templates/changes.md` to `pipeline/features/run-orchestrator-products/changes.md`, fill the Slug and Spec lines, and keep it updated as you work.
- Cycles C07 (API), C09 (verifier), C10 (skill) are being implemented concurrently and will import the `Run` and `RunStore` you write. Freeze this public surface and do not rename anything on it:
  - `class Run`: attributes `run_id: str`, `run_dir: Path`, `data_dir: Path`, `manifest: dict`, `scenario: Scenario`, `cube: HandCube`, `store: ForcingStore`, `record_start: datetime`, `record_end: datetime`, `max_horizon_minutes: int`; methods `resolve(p, t) -> ResolvedQuery`, `routed(p_internal: datetime) -> RoutedSeries`, `state(p, t, write: bool = False) -> tuple[StateArrays, dict]`, `reaches(p, t) -> pd.DataFrame`, `gauges(p) -> pd.DataFrame`, `tte(p, write: bool = False) -> np.ndarray`, `product_path(kind: str, p, t=None) -> Path`; classmethods `create(scenario, mode, overrides, runs_dir, data_dir, engine_version)` and `open(run_dir, data_dir)`. `p` and `t` accept contract ISO strings or tz-aware datetimes, and `p` may be the literal `"hindsight"`.
  - `class RunStore(runs_dir: Path, data_dir: Path, url_base: str = "/runs")`: `list() -> list[dict]`, `get(run_id) -> Run`, `create(scenario, mode, overrides=None) -> Run`, `exists(run_id) -> bool`.
- Use the merged modules with these exact signatures: `route(cube, view, scenario, config, members=MEMBERS, max_horizon_minutes=360, warmup_minutes=360) -> RoutedSeries`; `ParquetForcingView(scenario, store, p, network=None, gauges=None)` (pass `cube.network` and `cube.gauges`); `load_forcing_store(scenario, data_dir) -> ForcingStore`; `map_member(cube, q_by_feature, n_scale=1.0, with_velocity=True) -> MemberFields`; `reduce_members(low, mid, high, p=None, t=None, compute_ms=None) -> StateArrays`; `time_to_exceedance(iterable of (minutes_after_p, depth_mid)) -> ndarray[3, H, W]`; `write_depth_cog(path, grid, arrays)`, `write_tte_cog(path, grid, tte)` from `flood.products.raster`; `stage_from_q`, `wet_area` from `flood.engine.rating` are vectorised over numpy arrays of `cidx` and `q`.
- `route` already clamps the warm-up to `record.start` and drops taus after `record.end`. `RoutedSeries.at(t)` returns `q[3, R]` in `cube.network` row order; `RoutedSeries.source` is int8 codes decoded through `SOURCE_CODES`.
- `q_by_feature` for `map_member` is built from `routed.at(t)[member_index]` and `routed.feature_ids`; restrict the reach table to `cube.network.in_aoi` rows but map every reach.
- `config_hash` is `sha256` of `json.dumps(config, sort_keys=True, separators=(",", ":"))`; serialise the merged `ForcingConfig` with `model_dump(mode="json", exclude_none=True)` before hashing so defaults are stable.
- Products URLs in the state response are `f"{url_base}/{run_id}/raster?p=...&t=..."` and friends exactly as in `docs/contracts/examples/state-response.json`; the response must validate with `flood.contracts.validate.validate_json("state-response", ...)`.
- Fixture tests use `mini_scenario`, `mini_data_dir`, and a `tmp_path` runs dir: `Run.create(mini_scenario, "replay", None, tmp_path / "runs", mini_data_dir, "0.1.0")`.
- Kerr measurement for decision 0003: the main checkout's data directory at `..\Flood\data` already holds `cube\kerr-2025-07-04` (940 MB) and `hand\12100201`. Do not copy it. Fetch Kerr USGS forcing only (NWM is skipped to save time): `.venv\Scripts\flood.exe prep forcing scenarios\kerr-2025-07-04.json --data-dir ..\Flood\data --skip-nwm`. Then `flood run create scenarios\kerr-2025-07-04.json --runs-dir ..\Flood\runs --data-dir ..\Flood\data` and `flood run state <run_id> --p 2025-07-04T08:00:00Z --t 2025-07-04T10:00:00Z --write` plus two more pairs (`--p 2025-07-04T06:00:00Z --t 2025-07-04T06:00:00Z`, `--p hindsight --t 2025-07-04T09:00:00Z`). Paste every `compute_ms` block into changes.md and state which decision 0003 branch (under 300 ms, 300 ms to a few seconds, slower) the mapping stage falls in. If the Kerr run fails, record the traceback under Not done and continue; the fixture tests are the gate.
- Add exactly one line under the `# REGISTER:` marker in `src/flood/cli.py` registering `flood.cli_run`, in the same style as the existing lines.
- Acceptance criteria are the definition of done. Run pytest until green offline. If a criterion cannot be met, keep the test, mark it xfail with a reason, and record it under Not done.
- Finish: run `.venv\Scripts\python.exe -m pytest -q` and paste the output into changes.md under "How to verify".

Work order: `products/manifest.py` (`config_hash`, `make_run_id`, `merge_forcing`, `build_manifest`, `ENGINE_LIMITATIONS`) with tests; `products/tables.py` (`build_reaches`, `build_gauges`, JSON conversion) with tests; `engine/run.py` (`ResolvedQuery`, `Run`, `RunStore`) with the fixture tests; `cli_run.py`; the Kerr measurements.
