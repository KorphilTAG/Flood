Implement exactly the spec at the path below. Read it in full first, then read `docs/specs/physics-engine-cycles.md` (sections "File ownership" and "Interface freeze"), `src/flood/interfaces.py`, `src/flood/contracts/models.py`, `tests/conftest.py`, `tests/fixtures/mini_huc/build.py`, and every existing file the spec names. Follow "Files to change" and "Implementer notes" literally: exact paths, signatures, constants, and test names. Never edit `src/flood/interfaces.py`, `src/flood/engine/cube.py`, `tests/conftest.py`, or anything under `tests/fixtures/mini_huc/`. Do not edit files you do not own; if you need a change elsewhere, record it under Residual risk in changes.md. No scenario constants in `src/`: hosts live in the module constants the spec names, everything else comes from the scenario file. Do not commit, stage, or run any git command that changes state.

Spec: pipeline/features/forcing-ingest-nwm-usgs/spec.md

Environment:
- You are at the root of a git worktree on branch `feature/forcing-ingest-nwm-usgs`. Windows 11; use PowerShell syntax for shell commands.
- First create the environment: `py -3.12 -m venv .venv` then `.venv\Scripts\python.exe -m pip install -e ".[dev]"`. Use `.venv\Scripts\python.exe` for everything, including `-m pytest -q`.
- Copy `pipeline/templates/changes.md` to `pipeline/features/forcing-ingest-nwm-usgs/changes.md`, fill the Slug and Spec lines, and keep it updated as you work.
- Cycles C02, C03, C05, C08 are being implemented concurrently in other worktrees. Import only `flood.interfaces`, `flood.engine.cube`, `flood.contracts`, `flood.scenario`, `flood.timegrid`, `flood.timing`, and the modules you create.
- Signature clarification: `ParquetForcingView(scenario, store, p, network: pd.DataFrame | None = None, gauges: pd.DataFrame | None = None)`. `network` (the cube's `NETWORK_COLUMNS` frame) is needed for `ratio()` to walk levelpaths; when it is None, `ratio()` returns 1.0. `gauges` (the cube's `GAUGE_COLUMNS` frame) supplies `gauge_altitude_m` for `obs_wse`; when None, `obs_wse` returns gauge height without datum. Tests pass `mini_cube.network` and `mini_cube.gauges`.
- Fixture forcing lives in the production layout: `load_forcing_store(mini_scenario, mini_data_dir)` reads `<mini_data_dir>/usgs/mini-huc/continuous.parquet`, `<mini_data_dir>/nwm/mini-huc/analysis.parquet`, `<mini_data_dir>/nwm/mini-huc/short_range.parquet`. Use the `mini_scenario`, `mini_data_dir`, and `mini_cube` fixtures; never hardcode those paths.
- The synthetic channel_rt netCDF for `subset_file` tests is written at test time with xarray and the `h5netcdf` engine: dimension `feature_id`, coordinates `time` (length 1) and `reference_time` (length 1), variables `streamflow` stored as int32 with `scale_factor` 0.01, and `velocity`, `qSfcLatRunoff`, `qBucket` as float32. Do not commit netCDF files.
- The recorded USGS pages for the paging test: fetch them once from the real endpoint with `limit=5` for the first gauge and first hour of the `kerr_scenario` fixture's record, save the two JSON responses under `tests/fixtures/forcing/usgs_page1.json` and `usgs_page2.json`, and serve them in the test through `httpx.MockTransport`. If the network is unavailable, hand-write two minimal pages in the same shape and note it under Assumptions.
- Add exactly one line under the `# REGISTER:` marker in `src/flood/cli.py`, in the same style as the existing line, registering `flood.cli_prep_forcing`. The command is `flood prep forcing <scenario.json> [--data-dir data] [--keep-raw] [--skip-nwm] [--skip-usgs]`.
- Acceptance criteria are the definition of done. Run pytest until green offline. If a criterion cannot be met, keep the test, mark it xfail with a reason, and record it under Not done.
- After the offline suite is green, run `.venv\Scripts\python.exe -m pytest -m network tests/test_nwm.py::test_list_kerr_day -q` once and paste the output into changes.md. Do not run the full Kerr forcing download in this cycle.
- Finish: run `.venv\Scripts\python.exe -m pytest -q` and paste the output into changes.md under "How to verify".

Work order: `nwm.py` name builders and `subset_file` with the synthetic netCDF test; Parquet append and `manifest.json` skip logic; `usgs.py` fetch with the two-page mock test and unit conversion test; `engine/forcing.py` (`ForcingStore`, `load_forcing_store`, `ParquetForcingView`) with the availability, outage, and ratio tests on the fixture; `cli_prep_forcing.py`; the single network test.
