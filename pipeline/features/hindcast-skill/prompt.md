Implement exactly the spec at the path below. Read it in full first, then read `docs/specs/physics-engine-cycles.md` (sections "File ownership" and "Interface freeze"), `src/flood/interfaces.py`, `src/flood/engine/routing.py` (the `route` signature and `RoutedSeries`), `src/flood/engine/forcing.py` (`load_forcing_store`, `ParquetForcingView`), `src/flood/engine/mapping.py`, `src/flood/engine/ensemble.py`, `tests/conftest.py`, and every existing file the spec names. Follow "Files to change" and "Implementer notes" literally. You own only `src/flood/skill/**`, `src/flood/cli_skill.py`, one line in `src/flood/cli.py`, and `tests/test_skill.py`. Do not touch anything else; if you need a change elsewhere, record it under Residual risk in changes.md. No scenario constants in `src/`. Do not commit, stage, or run any git command that changes state.

Spec: pipeline/features/hindcast-skill/spec.md

Environment:
- You are at the root of a git worktree on branch `feature/hindcast-skill`. Windows 11; use PowerShell syntax for shell commands.
- First create the environment: `py -3.12 -m venv .venv` then `.venv\Scripts\python.exe -m pip install -e ".[dev]"`. Use `.venv\Scripts\python.exe` for everything, including `-m pytest -q`.
- Copy `pipeline/templates/changes.md` to `pipeline/features/hindcast-skill/changes.md`, fill the Slug and Spec lines, and keep it updated as you work.
- Cycle C06 is writing `flood.engine.run` concurrently; it does not exist in this worktree. `compute_skill` must rely only on this duck-typed surface of a run object, and must not import `flood.engine.run` at module level: `run.scenario` (Scenario), `run.cube` (HandCube), `run.data_dir` (Path), `run.run_dir` (Path), `run.record_start` and `run.record_end` (tz-aware datetimes), `run.max_horizon_minutes` (int), `run.routed(p_internal: datetime) -> RoutedSeries`, and `run.state(p, t, write=False) -> tuple[StateArrays, dict]` where `p` may be the literal `"hindsight"`.
- In `tests/test_skill.py`, build a `_FixtureRun` class with exactly that surface from the merged modules: `routed(p)` calls `route(cube, ParquetForcingView(scenario, store, p, network=cube.network, gauges=cube.gauges), scenario, scenario.forcing_defaults, max_horizon_minutes=360)`; `state(p, t)` maps the three members with `map_member(cube, {fid: q ...}, with_velocity=(member == "mid"))` from `routed.at(t)` and reduces with `reduce_members(low, mid, high, p=..., t=...)`; `"hindsight"` means `p = record_end`. Cache routed series per `p` inside the fake. Use the `mini_scenario`, `mini_data_dir`, `mini_cube` fixtures and `load_forcing_store(mini_scenario, mini_data_dir)`.
- Observations for truth and persistence come from `ParquetForcingView`: truth from a view at `run.record_end`, persistence from a view at `p` (last known value of `obs_q(site)`). Gauge sites and roles come from `run.scenario.hydrology.gauges`; the reach of each gauge from `run.cube.network.gauge_site`.
- Functions and files: `skill_rows(run, p, horizons) -> list[dict]`, `compute_skill(run, cutoffs, horizons, iou_threshold_m=0.15) -> pd.DataFrame`, `summarise(df) -> pd.DataFrame`, `write_skill(run_dir, detail_df, summary_df) -> tuple[Path, Path]` writing `skill.parquet` (summary rows, this is what the API serves) and `skill_detail.parquet`, and `update_limitations(run_json_path, lines: list[str]) -> None` that appends to `limitations` and validates the result with `flood.contracts.validate.validate_json("run-manifest", ...)`. Test `update_limitations` on a copy of `docs/contracts/examples/run.json` in `tmp_path`.
- `target_check(summary_df, scenario) -> list[str]` returns the limitation lines the spec describes for interior gauges at horizons 60 and 120.
- IoU only at horizons 60 and 120, comparing `depth_mid >= threshold` between `run.state(p, t)` and `run.state("hindsight", t)`, ignoring NaN cells.
- Fixture test expectations (`p` every 60 minutes from 01:00Z to 09:00Z, horizons 0, 30, 60): horizon-0 rows at gauge sites 90000001 and 90000003 have `abs_error_cms < 1e-3` because observations are hard controls; `summarise` has one row per gauge and horizon; both Parquet files are written; `skill` is finite where `persistence_mae_cms > 0`.
- `cli_skill.py`: `flood skill <run_id> [--runs-dir runs] [--data-dir data] [--cutoff-step-min 30] [--horizons 0,30,60,120,240]`. It needs `flood.engine.run.RunStore` to open the run; import it inside the function and print `error: engine run store not available` with exit 2 if the import fails. Add exactly one line under the `# REGISTER:` marker in `src/flood/cli.py`, in the same style as the existing lines. The CLI test is guarded by `pytest.importorskip("flood.engine.run")` and will skip here.
- Acceptance criteria are the definition of done. Run pytest until green offline. If a criterion cannot be met, keep the test, mark it xfail with a reason, and record it under Not done.
- Finish: run `.venv\Scripts\python.exe -m pytest -q` and paste the output into changes.md under "How to verify".

Work order: `skill/hindcast.py` (`skill_rows`, `compute_skill`, `summarise`, `target_check`, `write_skill`, `update_limitations`); `tests/test_skill.py` with `_FixtureRun`; `cli_skill.py` and the cli.py line.
